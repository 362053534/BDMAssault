/* The embedded ata_bd.irx exports the standard ATAD ABI. This outer module
 * only loads DEV9 and ata_bd.irx, so it exports its own minimal ABI. */
DECLARE_EXPORT_TABLE(bdm_hdd, 1, 1)
	DECLARE_EXPORT(_start)
	DECLARE_EXPORT(_retonly)
END_EXPORT_TABLE

void _retonly() {}
